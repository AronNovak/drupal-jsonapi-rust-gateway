[server]
host = "0.0.0.0"
port = 3000
base_url = "http://localhost:3000"

[database]
url = "mysql://db:db@127.0.0.1:33001/db"
pool_size = 10

[drupal]
backend_url = "https://drupal.ddev.site"
jsonapi_prefix = "/jsonapi"

[cache]
collection_max_age = 60
individual_max_age = 300

[pagination]
default_limit = 50
max_limit = 50
